const anything = {
  schema:
    'In this JSON file, you must define your own schema for information components and display it',
  usability:
    'This folder is only used if you do not have a backend or a database service, or if you require everything to be managed locally',
};

export const data = [anything /* ...rest of objects with the information */];
